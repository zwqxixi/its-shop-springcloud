server:
  port: 8004
spring:
  application:
    name: its-shop-zipkin
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    com.its: DEBUG